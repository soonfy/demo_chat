<%- include header %>

  <%- include nav %>

    <link rel='stylesheet' href='/stylesheets/list.css' />

    <div class="container">
      <div class="left">
        <!--<div>
          <span>热门</span>
          <p><a href="/blog.html">msg聊天室目前只支持中英文输入，不支持emoji表情。</a></p>
          <p><a href="/blog.html">msg聊天室目前只支持中英文输入，不支持emoji表情。</a></p>
          <p><a href="/blog.html">msg聊天室目前只支持中英文输入，不支持emoji表情。</a></p>
          <p><a href="/blog.html">msg聊天室目前只支持中英文输入，不支持emoji表情。</a></p>
          <p><a href="/blog.html">msg聊天室目前只支持中英文输入，不支持emoji表情。</a></p>
        </div>-->
        <div>
          <span>标签</span>
          <% for(var key in keyCount){ %>
            <p><a href="/blog/key/<%= key %>.html" title="<%= keyCount[key] %> 篇文章"><%= key %></a></p>
          <% } %>
        </div>
        <!--<div>
          <span>分类</span>
          <p><a href="/blog.html">msg聊天室目前只支持中英文输入，不支持emoji表情。</a></p>
          <p><a href="/blog.html">msg聊天室目前只支持中英文输入，不支持emoji表情。</a></p>
          <p><a href="/blog.html">msg聊天室目前只支持中英文输入，不支持emoji表情。</a></p>
        </div>
        <div>
          <span>热门</span>
          <p><a href="/blog.html">msg聊天室目前只支持中英文输入，不支持emoji表情。</a></p>
          <p><a href="/blog.html">msg聊天室目前只支持中英文输入，不支持emoji表情。</a></p>
          <p><a href="/blog.html">msg聊天室目前只支持中英文输入，不支持emoji表情。</a></p>
          <p><a href="/blog.html">msg聊天室目前只支持中英文输入，不支持emoji表情。</a></p>
        </div>-->
      </div>
      <% if (results.length > 0){ %>
        <div class="right">
          <% for(var index = 0, len = results.length; index < len; index++){ %>
            <div>
              <p>
                <a href="/blog/<%= results[index]._id %>.html"><%- results[index].title.replace(new RegExp(keyword, 'igm'), str => `<strong class="highlight">${str}</strong>`) %></a>
                <span><%= results[index].score %></span>
              </p>
              <p>
                <%- results[index].summary.replace(new RegExp(keyword, 'igm'), str => `<strong class="highlight">${str}</strong>`) %>
              </p>
              <span><%= results[index].updatedAt %></span><span><%- results[index].key.join(';').replace(new RegExp(keyword, 'igm'), str => `<strong class="highlight">${str}</strong>`) %></span>
            </div>
            <% } %>
        </div>
        <% } %>
    </div>

    <%- include footer %>
